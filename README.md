# 🚀 Node.js Alert System

A Node.js application that integrates **Google OAuth2.0**, **Google Calendar API**, and **Twilio** for event alerts and notifications.  
Built with **Express**, **TypeScript**, and **MongoDB**.
# git Clone 
# npm install (only first time)
# npm run dev (during development)

npm run build && npm start (for production)
